[package]
name = "rust-knowledge-tester"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { version = "0.7", features = ["desktop"] }
rand = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
bundle = []
